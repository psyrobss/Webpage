/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/
import * as THREE from 'three'
import React, { useEffect } from 'react'
import { useGraph } from '@react-three/fiber'
import { useGLTF, useAnimations } from '@react-three/drei'
import { GLTF, SkeletonUtils } from 'three-stdlib'

type ActionName = 'CharacterArmature|Bite_Front' | 'CharacterArmature|Dance' | 'CharacterArmature|Death' | 'CharacterArmature|HitRecieve' | 'CharacterArmature|Idle' | 'CharacterArmature|Jump' | 'CharacterArmature|No' | 'CharacterArmature|Walk' | 'CharacterArmature|Yes'
interface GLTFAction extends THREE.AnimationClip { name: ActionName }

type GLTFResult = GLTF & {
  nodes: {
    Green_Blob001_1: THREE.SkinnedMesh
    Green_Blob001_2: THREE.SkinnedMesh
    Green_Blob: THREE.SkinnedMesh
    Body: THREE.Bone
    Head: THREE.Bone
  }
  materials: {
    Eye_White: THREE.MeshStandardMaterial
    Eye_Black: THREE.MeshStandardMaterial
    Green_Main: THREE.MeshStandardMaterial
  }
  animations: GLTFAction[]
}

type GreenBlobModelProps = React.ComponentPropsWithoutRef<'group'>;

export default function GreenBlobModel(props: GreenBlobModelProps) {
  const group = React.useRef<THREE.Group>(null)
  const { scene, animations } = useGLTF('/models/Green Blob-transformed.glb') as unknown as GLTFResult
  const clone = React.useMemo(() => SkeletonUtils.clone(scene), [scene])
  const { nodes, materials } = useGraph(clone) as unknown as GLTFResult
  const { actions, names } = useAnimations(animations, group)

  useEffect(() => {
    const actionToPlay = names.find(name => name.includes('Idle') || name.includes('Walk')) || names[0];
    if (actionToPlay && actions[actionToPlay]) {
      actions[actionToPlay]?.reset().fadeIn(0.5).play();
      return () => { actions[actionToPlay]?.fadeOut(0.5); };
    }
  }, [actions, names]);

  return (
    <group ref={group} {...props} dispose={null}>
      <group name="Root_Scene">
        <group name="CharacterArmature" rotation={[-Math.PI / 2, 0, 0]} scale={100}>
          <primitive object={nodes.Body} />
          <primitive object={nodes.Head} />
        </group>
        <group name="Green_Blob001" rotation={[-Math.PI / 2, 0, 0]} scale={100}>
          <skinnedMesh castShadow receiveShadow name="Green_Blob001_1" geometry={nodes.Green_Blob001_1.geometry} material={materials.Eye_White} skeleton={nodes.Green_Blob001_1.skeleton} />
          <skinnedMesh castShadow receiveShadow name="Green_Blob001_2" geometry={nodes.Green_Blob001_2.geometry} material={materials.Eye_Black} skeleton={nodes.Green_Blob001_2.skeleton} />
        </group>
        <skinnedMesh castShadow receiveShadow name="Green_Blob" geometry={nodes.Green_Blob.geometry} material={materials.Green_Main} skeleton={nodes.Green_Blob.skeleton} rotation={[-Math.PI / 2, 0, 0]} scale={100} />
      </group>
    </group>
  )
}
useGLTF.preload('/models/Green Blob-transformed.glb')